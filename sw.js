// This empty fetch handler is the "magic key" that tricks Chrome 
// into recognizing the app as a fully installable PWA.
self.addEventListener('fetch', function(event) {
    // Leave this blank for now
});